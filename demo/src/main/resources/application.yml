spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Queeniechenjiaqi/firstWork.git
#          username:
#          password:


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8760/eureka/

server:
  port: 8888

