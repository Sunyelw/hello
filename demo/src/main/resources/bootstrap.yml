spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Queeniechenjiaqi/firstWork.git
          label: master
          fastFail: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8760/eureka/

server:
  port: 8888

